# zmux Porting Todo

Implementation gaps where zmux behaviour diverges from tmux due to incomplete
porting.  "Porting todo" and "porting bug" are synonymous here: the acceptance
criterion is full parity, so every row in this table represents unfinished work
regardless of current severity.

Append new rows as gaps are discovered.  Do not remove a row until the fix has
landed and been tested.

| Gap | Location | Remediation | Prerequisites |
|-----|----------|-------------|---------------|
| `#{sixel_support}` always returns `"0"` — stale comment claims zmux has no sixel support, but `TERM_SIXEL` feature detection and sixel draw path are both implemented | `src/format-resolve.zig:2379` `fn resolve_sixel_support` | Return `"1"` when `ctx.client?.term_features & tty_features.TERM_SIXEL != 0`; return `null` (not `"0"`) when there is no client context, consistent with other client-gated resolvers | None — `TERM_SIXEL` flag and `client.term_features` already exist |
| `#{client_written}` always returns `"0"` — `Client` struct has no cumulative bytes-written counter; resolver is an acknowledged stub | `src/format-resolve.zig:2292` `fn resolve_client_written` | 1. Add `written: u64 = 0` field to `Client` in `types.zig`. 2. Increment `cl.written` in the tty flush path (tmux does this in `tty_write_flush`). 3. Update resolver to `return xm.xasprintf("{d}", .{cl.written})` | Identify and instrument the correct write-flush callsite in `src/tty.zig` or `src/proc.zig`; grep for `tty_write_flush` equivalent |
| `screen_alternate_on/off` are dead code — `screen_write_alternateon/off` wrappers call the older `screen_enter/leave_alternate` (WindowPane-level, simple grid pointer swap) instead of the new tmux-faithful `screen_alternate_on/off` (Screen-level, full grid copy + GRID_HISTORY management) | `src/screen-write.zig:1847,1853` `screen_write_alternateon`, `screen_write_alternateoff`; new functions at `src/screen.zig:516,541` | Update `screen_write_alternateon` to call `screen_alternate_on(ctx.s, gc, cursor != 0)` and `screen_write_alternateoff` to call `screen_alternate_off(ctx.s, gc, cursor != 0)`; verify alternate-screen option check mirrors tmux (`alternate-screen` option, `tmux-museum/src/screen-write.c:2463`) | None |
| Selection system not wired into window-copy — `screen_set_selection` and `screen_hide_selection` are never called from `window-copy.zig`; tmux calls them at `window-copy.c:4982` and `window-copy.c:4973` | `src/window-copy.zig`; functions at `src/screen.zig` `fn screen_set_selection`, `fn screen_hide_selection` | Wire `screen_set_selection` calls into copy-mode selection start/update path and `screen_hide_selection` into copy-mode deactivation path, mirroring `tmux-museum/src/window-copy.c` | None — functions are implemented; callsites are missing |
| Selection not checked or rendered — `screen_check_selection` never called from `screen-write.zig`; `screen_select_cell` never called from `tty-draw.zig`; tmux calls them at `screen-write.c:2142` and `tty-draw.c:190,250` respectively | `src/screen-write.zig`; `src/tty-draw.zig`; functions at `src/screen.zig` `fn screen_check_selection`, `fn screen_select_cell` | Add selection-check path to cell-write loop in `screen-write.zig` and to cell-draw loop in `tty-draw.zig`, mirroring tmux reference | Selection system wired into window-copy (row above) |
| `screen_set_cursor_colour` never called from input processing — tmux calls it at `input.c:3060` and `input.c:3068` when handling `DECSCUSR` colour parameter | `src/input.zig`; function at `src/screen.zig` `fn screen_set_cursor_colour` | Find the DECSCUSR / OSC cursor-colour handler in `src/input.zig` and add the `screen_set_cursor_colour` call, mirroring `tmux-museum/src/input.c:3060,3068` | None |
| `screen_mode_to_string` never called in zmux — tmux uses it for debug logging in `tty.c:875,877`, `screen-write.c:884,896`, `server-client.c:2991` | `src/screen.zig` `fn screen_mode_to_string` | Wire into equivalent debug/log paths in zmux once those paths are ported; low urgency, diagnostic only | Depends on debug logging infrastructure being ported |
| DECSCUSR escape sequence does not call `screen_set_cursor_style` — applications running in panes cannot change cursor appearance (shape/blink) via escape sequences; tmux handles this at `input.c:1805` | `src/input.zig`; function at `src/screen.zig` `fn screen_set_cursor_style` | Find the CSI DECSCUSR handler in `src/input.zig` and add `screen_set_cursor_style` call, mirroring `tmux-museum/src/input.c:1805` | None — distinct from cursor colour gap (separate DECSCUSR parameter) |
| Copy-mode backing screen missing default cursor initialisation — `window_copy_common_init` creates the backing screen but never calls `screen_set_default_cursor`; tmux does this at `window-copy.c:432` | `src/window-copy.zig` `fn window_copy_common_init` (around screen creation); function at `src/screen.zig` `fn screen_set_default_cursor` | Add `screen_set_default_cursor(data.backing, opts.global_w_options)` after `screen_init` in `window_copy_common_init` | None |
| Popup screens missing default cursor initialisation — popup screen creation calls `screen_init` but never calls `screen_set_default_cursor`; tmux does this at `popup.c:829` | `src/popup.zig` (screen_init callsites); function at `src/screen.zig` `fn screen_set_default_cursor` | Add `screen_set_default_cursor(&pd.s, opts.global_w_options)` after each `screen_init` call in popup creation paths | None |
| Mouse clicks on status bar not routed to correct pane/window/session — `server_client_check_mouse` sets `where = .status_default` without calling `status_get_range()`; tmux parses status line ranges at `server-client.c:797-845` to target specific items | `src/server-client.zig` `fn server_client_check_mouse` (~line 1714) | Port `status_get_range()` call and range-checking logic from `tmux-museum/src/server-client.c:797-845`; populate correct `.where` variant from the range result | `status_get_range` must be ported first (currently absent from zmux) |
| Double-click not emitted — `server_client_click_timer` callback cancels the timer but never emits `KEYC_DOUBLECLICK`; tmux emits it at `server-client.c:3084-3096` when awaiting triple-click | `src/server-client.zig` `fn server_client_click_timer` | Add logic: if waiting for triple-click, call `server_client_handle_key(c, event)` with `KEYC_DOUBLECLICK`; mirror `tmux-museum/src/server-client.c:3084-3096` | None — mouse infrastructure exists |
| Resize timer callback does not flush the resize queue — `server_client_resize_timer` only deletes the libevent, missing the resize-queue processing that tmux does; `server_client_check_pane_resize` exists but is not called from the timer | `src/server-client.zig` `fn server_client_resize_timer` | Call `server_client_check_pane_resize()` (or equivalent) from inside the timer callback before deleting the event, mirroring tmux's timer handler | None |
| `control_start` never called when CLIENT_CONTROL is identified — control mode state is never initialized; tmux calls it from `server_client_dispatch_identify()` when the `CLIENT_CONTROL` flag is set; zmux just closes the terminal FDs and returns | `src/server-client.zig` ~line 480 (`server_client_finalize_identify`); `fn control_start` in `src/control.zig` | Add `control.control_start(cl)` call in the CLIENT_CONTROL branch of `server_client_finalize_identify`, mirroring `tmux-museum/src/server-client.c` | None — `control_start` is fully implemented |
| `control_read_callback` has no caller — control client stdin is routed through the TTY key path instead of being parsed as line-based control commands; `control_read_callback` is fully implemented but unreachable | `src/server-client.zig` stdin dispatch path (~line 260); `fn control_read_callback` in `src/control.zig:646` | In `server_client_dispatch_stdin` (or equivalent), check `CLIENT_CONTROL` flag; accumulate stdin bytes until `\n`; call `control_read_callback(cl, line)` for each complete line | `control_start` must be wired first (row above) |
| `control_write_callback` never called from event loop — pending output blocks are never flushed; in tmux this is driven by libevent bufferevent write-ready events | `src/control.zig` `fn control_write_callback`; event loop in `src/server.zig` or `src/proc.zig` | Call `control_write_callback(cl)` when the client peer's write buffer becomes writable (equivalent of bufferevent write callback); or call it from the server dispatch loop after processing each command | Control stdin dispatch (row above) |
| Tab completion never shows candidate list — `status_prompt_complete_list_menu` is a no-op; pressing Tab shows the prefix match but never pops up the completion menu; tmux uses `menu_create()` + `menu_display()` at `status.c:1916-1965` | `src/status-prompt.zig:1640` `fn status_prompt_complete_list_menu`; called from `src/status-prompt.zig:1692` | Create menu with `menu_create()`, populate with `menu_add_item()`, display with `menu_display()` and `status_prompt_menu_callback`; menu infrastructure now exists (added by later commits) | Menu infrastructure must be available (already merged) |
| `mode_tree_display_help` is a no-op — pressing F1/Ctrl+H in choose-tree/choose-buffer/choose-client shows nothing; tmux calls `popup_display()` with help text at `mode-tree.c:1186` | `src/mode-tree.zig:1007` `fn displayHelp` | Call `popup_display()` with formatted key-binding help text, mirroring `tmux-museum/src/mode-tree.c:1186` | Popup display infrastructure must support help text callbacks |
| mode-tree search and filter prompts do nothing — pressing `/`, `Ctrl+S` (search) or `f` (filter) in choose-tree calls `searchCallback`/`filterCallback` which just release a ref; tmux invokes `status_prompt_set()` with the appropriate callback | `src/mode-tree.zig:1042` `fn searchCallback`; `src/mode-tree.zig:1049` `fn filterCallback` | Call `status_prompt_set()` with `mode_tree_search_callback` / `mode_tree_filter_callback`, mirroring `tmux-museum/src/mode-tree.c`; both internal callbacks (`searchCallbackInternal`, `filterCallbackInternal`) are already implemented and ready to receive the result | `status_prompt_set` must be callable from mode-tree context |
| `file_write_callback`, `file_read_callback` and error variants not using libevent bufferevent — zmux does synchronous fd I/O instead; will block on slow writes and miss async read readiness signals; `file.zig:230` explicitly notes the gap | `src/file.zig:530,545,564,589` (`fileWriteErrorCallback`, `fileWriteCallback`, `fileReadErrorCallback`, `fileReadCallback`) | Replace direct `read()`/`write()` with `bufferevent_new()` + `bufferevent_setcb()` wired to the libevent base; mirror `tmux-museum/src/file.c` bufferevent setup | libevent bufferevent layer needs to be integrated into zmux's event loop first |
| `server_client_key_table_activity_diff` has structural gap — zmux `Client` stores `key_table_name: []u8` (string) while tmux uses a direct `keytable: *KeyTable` pointer with an `activity_time` field; function is also unreachable (dead-code cascade from `server_client_key_callback` not being wired) | `src/server-client.zig` `fn server_client_key_table_activity_diff`; `src/types.zig` `Client` struct | 1. Decide whether to adopt the callback dispatch model or keep the direct `server_client_handle_key` approach. 2. If keeping direct dispatch, add `key_table_activity_time` field to `Client` and compute diff there. 3. If adopting callback model, wire `server_client_key_callback` into the event loop | Architectural decision: callback dispatch model vs. direct key handling |
